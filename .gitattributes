# Normalize all text files to LF in the repository (ZOHO Books CSV output
# requires Unix LF, and the shell wrappers must stay LF to run under bash).
* text=auto eol=lf

# Binary formats — never touch line endings
*.xls binary
*.xlsx binary
